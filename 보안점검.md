# 보안 점검 기록 (2026-07-22)

`securecondhand`를 GitHub에 올린 뒤 붙인 자동 취약점 스캔 도구와, 그 결과로 나온 의존성 업데이트를 실제로 검토·테스트·반영한 기록이다.

---

## 1. 붙인 도구

| 도구 | 역할 | 설정 파일 |
|---|---|---|
| **GitHub CodeQL (Code Scanning)** | 코드 자체의 취약점(SAST) — push/PR마다 + 매주 월요일 자동 스캔 | `.github/workflows/codeql.yml` |
| **Dependabot** | `package.json`(npm), `backend/requirements.txt`(pip), GitHub Actions의 알려진 취약 버전을 찾아 업데이트 PR을 자동 생성 | `.github/dependabot.yml` |

두 도구 모두 GitHub 네이티브 기능이라 별도 계정/설치 없이 저장소 Settings만으로 동작한다. 결과는 저장소의 **Security → Code scanning alerts**(CodeQL)와 **Pull requests**(Dependabot) 탭에서 확인한다.

---

## 2. Dependabot이 연 PR과 처리 결과

붙이자마자 알려진 취약/구버전 의존성 12건에 대해 PR이 자동 생성됐다. 전부 로컬에서 실제로 설치·빌드·기동까지 확인한 뒤 반영 여부를 결정했다.

### 2.1 반영 완료 (저위험 — 마이너/패치 버전업)

| PR | 변경 | 검증 |
|---|---|---|
| #1 | `actions/checkout` 4→7 | GitHub Actions 내부 도구, 앱 코드 영향 없음 |
| #2 | `github/codeql-action` 3→4 | 〃 |
| #4 | `channels` 4.0→4.3.2 | `manage.py check` + 서버 기동 + API 응답 200 확인 |
| #6 | `psycopg` 3.1→3.3.4 | 〃 |
| #10 | `djangorestframework` 3.15→3.17.1 | 〃 |
| #11 | `django-cors-headers` 4.3→4.9.0 | 〃 |

### 2.2 반영 완료 (고위험 — 메이저 버전업, 코드 수정 필요했던 것)

| PR | 변경 | 이슈 및 조치 |
|---|---|---|
| #8 | Django 5.1→6.0.7 | Django 6.0에서 `CheckConstraint(check=...)` 인자가 제거되고 `condition=...`으로 바뀌어 앱이 기동조차 안 됐음. `backend/marketplace/models.py`의 `FriendRequest` 제약 조건 1곳을 `condition=`으로 수정 후 `manage.py check`·마이그레이션 확인·서버 기동·API 응답까지 정상 확인. |
| #9, #3 | `react`/`react-dom` 18→19.2.8 | 이미 `ReactDOM.createRoot` API(React 19 호환)를 쓰고 있어 코드 수정 없이 통과. `npm run build` + dev 서버 기동 확인. |
| #7 | `react-router-dom` 6→7.18.1 | 코드에서 쓰는 API(`Routes`/`Route`/`useNavigate`/`useParams`/`Link`/`HashRouter`)가 모두 v7에서도 그대로 유지되는 안정 API라 수정 없이 통과. |

### 2.3 보류 (환경 제약으로 아직 반영 불가)

| PR | 변경 | 보류 사유 |
|---|---|---|
| #12 | `vite` 5→8.1.5 | vite 8의 번들러(rolldown)가 `node:util`의 `styleText`를 import하는데, 이는 **Node.js 20+에서만 제공**되는 API. 현재 개발 환경 Node 버전은 18.19.1이라 `npm run build` 실행 시 `SyntaxError: ... does not provide an export named 'styleText'`로 즉시 크래시. |
| #5 | `@vitejs/plugin-react` 4→6.0.3 | vite 8과 짝을 이루는 패키지라 동일 사유로 보류. |

→ 두 PR은 **Node.js를 20 이상으로 올린 뒤** 다시 시도해야 한다. GitHub PR 목록에는 열린 채로 남아있으니, 이 저장소를 다루는 환경의 Node 버전을 올리게 되면 이 문서를 참고해 마저 진행할 것.

> 참고: PR #9/#3/#7은 Dependabot이 만든 브랜치를 그대로 merge하지 않고 `package.json`/`package-lock.json`을 직접 맞춰 커밋했다. 따라서 GitHub이 자동으로 PR을 닫아주지 않으므로, 웹에서 해당 3개 PR을 "이미 다른 커밋으로 반영됨" 사유로 수동으로 닫아야 한다.

---

## 3. 결론

- 저장소에 **CodeQL + Dependabot**을 상시 가동 상태로 붙였다. 앞으로 push/PR마다 코드 취약점 스캔이, 매주 의존성 취약점 점검이 자동으로 돈다.
- 이번 1차 점검에서 나온 12건 중 **10건은 실제로 설치·빌드·기동까지 검증 후 반영**했고, 그 과정에서 Django 6 비호환 코드(`CheckConstraint`)를 실제로 발견해 고쳤다.
- **2건(vite 8, plugin-react 6)은 이 개발 환경의 Node.js 버전(18) 한계로 보류** — 코드 문제가 아니라 인프라 문제이므로, Node 업그레이드가 선행되어야 한다.
