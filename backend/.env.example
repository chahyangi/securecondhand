DJANGO_SECRET_KEY=change-this-secret-key
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=localhost,127.0.0.1

POSTGRES_DB=securecondhand
POSTGRES_USER=securecondhand
POSTGRES_PASSWORD=securecondhand
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# 토스페이먼츠 결제위젯 테스트 키 (developers.tosspayments.com 가입 후 API 키 메뉴에서 발급,
# 사업자등록 불필요). 지갑 충전 기능에서만 사용하며, 실제 결제는 발생하지 않는다.
TOSS_CLIENT_KEY=발급받은_test_gck로_시작하는_값으로_교체
TOSS_SECRET_KEY=발급받은_test_gsk로_시작하는_값으로_교체
